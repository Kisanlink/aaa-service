package roles

import resp "github.com/Kisanlink/aaa-service/internal/entities/responses"

// ErrorResponseDoc is a local alias to expose the error model to swag in this package
type ErrorResponseDoc = resp.ErrorResponse
