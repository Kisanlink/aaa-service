meta {
  name: Add user to group in organization
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/organizations/:orgId/groups/:groupId/users
  body: none
  auth: inherit
}

params:path {
  orgId:
  groupId:
}
